{
  "name": "auspex",
  "private": true,
  "type": "module",
  "description": "Warhammer 40,000 army builder, exact combat math engine, and list DSL",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=22",
    "bun": ">=1.3"
  },
  "packageManager": "bun@1.3.13"
}
