import { common } from '@/components/locales/en'
import { landing } from '@/features/landing/locales/en'
import { simulator } from '@/features/simulator/locales/en'
import { theme } from '@/theme/locales/en'

/** English resources, one namespace per feature plus shared `common`. */
export const en = { common, landing, simulator, theme } as const
